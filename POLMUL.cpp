#include <bits/stdc++.h>

using namespace std;

void solve(){
    int n, m;
    cin >>n >> m;
    int p[n], q[m];
    for(int i = n - 1;i >= 0;i--)    cin >> p[i];
    for(int i = m - 1;i >= 0;i--)    cin >> q[i];
    
}

int main(){
    int t;
    cin >> t;
    while(t--){
        solve();
    }
    return 0;
}